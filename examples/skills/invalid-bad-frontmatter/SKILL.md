# Bad Frontmatter Example

This example intentionally omits YAML frontmatter so validation tests can assert `skill-md-missing-frontmatter`.

Use a real skill package with frontmatter for publishable examples.
